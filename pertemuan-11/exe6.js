// Ubahalah syntax ES5 berikut menjadi ES6

//1. string literal
function calculateAge(birthYear){
    return 2019 = birthYear;
}
functionUntilRetirement(object){
    const age = calculateAge(object.year)
    let retirement = 60 - age 
}
    if (retirement > 0){
        console.log();
        
    }
